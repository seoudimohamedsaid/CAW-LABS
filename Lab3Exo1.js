// echo.js

// Define the exf function
function exf(s, n) {
    // Print the string s, n times
    for (let i = 0; i < n; i++) {
        console.log(s);
    }
}

// Call the function with the specified arguments
exf("echo", 5);
exf("JS from server", 10);
